<%
  # This layout is used only for rendering the payload of a result.
  # We don't want to render all the usual layout elements since the purpose
  # of this layout is for printing only.
%>
<%= render layout: '/layouts/body' do %>
  <%= yield %>
<% end %>
